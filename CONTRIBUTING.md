# 贡献指南

感谢你帮助改进这个零基础 PDF RAG 教程。最有价值的贡献通常是可以复现的错误报告、让某一步更容易理解的文档修改，以及带测试的小型修复。

## 开始之前

请先搜索现有 Issue。程序出错时使用 Bug 报告模板；教程内容没看懂时使用学习问题模板，并指出具体卡在哪一步。不要只写“不能运行”，也不要公开私人数据。

## 本地安装

需要 Python 3.11 或更高版本。克隆仓库后，在项目根目录创建隔离环境：

```sh
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -e ".[test]"
```

Windows PowerShell 的激活命令是：

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -e ".[test]"
```

## 修改与测试

保持改动最小且只解决一个明确问题。新增行为或修复错误时，先添加一个会因该问题失败的离线测试，再实现最小修复。默认测试不能依赖网络、Ollama 服务或付费 API。

提交前运行完整测试：

```sh
python -m pytest -q
```

若修改了 shell 脚本，再运行 `bash -n 路径/脚本名.sh`。若修改了公开命令、配置或学习步骤，也要同步更新对应文档，并从头复制执行相关命令。

## 代码风格

- 与相邻代码保持一致：Python 使用 4 个空格、清楚的名称和必要的类型标注。
- 优先写直接、短小的函数，不为首版没有的需求增加框架或抽象。
- 测试名称应说明行为；错误信息应告诉初学者下一步怎么做，且不能泄露配置。
- 文档使用面向初学者的简明中文，第一次出现的术语要解释；不要宣称未验证的性能或效果。
- 不做与当前修复无关的格式化或重构。

本项目没有额外的自动格式化命令；请以现有文件风格和完整测试结果为准。

## 隐私与密钥

提交 Issue、日志、截图、测试 fixture 或 Pull Request 前，请确认：

- 没有 API key、访问令牌、Cookie 或完整 `.env` 内容；
- 没有私人 PDF 原文、简历、公司内部资料或其他受限内容；
- 已将用户名、本机绝对路径、邮箱和可识别信息打码；
- 测试只使用仓库已有的开放许可示例或自行编写的虚构小文本。

如果密钥已经公开，请立即到服务商控制台撤销并重新生成；只从 Git 中删除并不能让旧密钥恢复安全。

## 提交 Pull Request

在描述中说明“为什么改、改了什么、如何验证”，并填写 PR 检查清单。维护者会优先接受范围清晰、测试可重复、不会增加默认网络依赖的改动。
